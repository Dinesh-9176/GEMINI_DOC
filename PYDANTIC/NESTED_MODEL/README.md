# Nested Pydantic Models

This directory demonstrates how to use nested models and lists of models within a Pydantic schema.

## Files
- `nested.py`: Demonstrates complex data structures with nested `BaseModel` classes.
