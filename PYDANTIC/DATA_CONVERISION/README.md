# Pydantic Data Conversion

This directory demonstrates Pydantic's data conversion and coercion capabilities. It shows how Pydantic automatically converts types where possible (e.g., float to int, string to int).

## Files
- `conversion.py`: Demonstrates type coercion and using `model_dump()`.
