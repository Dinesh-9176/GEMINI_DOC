# Basic Pydantic Model

This directory contains a basic example of using Pydantic's `BaseModel` to define a schema and validate data.

## Files
- `app.py`: Demonstrates defining a model and creating an instance with data.
