# Pydantic Validation Errors

This directory demonstrates how to catch and handle Pydantic's `ValidationError` when data does not conform to the defined schema.

## Files
- `validation.py`: Demonstrates use of `try...except` block with `ValidationError`.
